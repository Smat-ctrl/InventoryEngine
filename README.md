# InventoryEngine
A smart inventory web app that helps users plan what to build by comparing their current inventory with required items, then generates a “need to buy” list and finds the best prices by scraping selected retailers, all within a clean dark-mode interface.
